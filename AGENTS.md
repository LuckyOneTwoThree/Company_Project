# AGENTS.md — AI 协作规范

> 这是一个 PM 的工作区,不是自动化框架。AI 在这里只做 PM 明确要求的读写和检索。
> 加载时机:首次介入本工作区时先读本文件。

## 工作区定位

PM(人)主导,AI 辅助。所有记录由 PM 决定是否写入,AI 不主动跑流程、不编造决策、不自动归档。

## 文件读写约定

| 文件 | 何时读 | 何时写 |
|------|------|------|
| `现状.md` | PM 让你回顾当下时 | PM 明确说"更新现状"时,**覆盖写**(不 append) |
| `记忆/进度日志.md` | PM 让你回顾进度时 | PM 明确说"记一下进度"时,**倒序插入最上方** |
| `记忆/决策记录.md` | PM 让你查历史决策时 | PM 明确说"记一个决策"时,**倒序插入最上方** |
| `记忆/沟通记录.md` | PM 让你查某次沟通时 | PM 明确说"记这次沟通"时,**倒序插入最上方** |
| `记忆/知识库.md` | PM 让你查经验/坑点时 | PM 明确说"沉淀一个经验"时,**倒序插入对应小节表格最上方** |
| `项目管理/*` | PM 让你查项目信息时 | PM 明确要求更新时 |
| `文档库/**` | PM 让你查文档时 | PM 明确要求起草/更新时;子目录按流程顺序编号:01-需求/02-调研/03-方案/04-评审/05-会议/06-验收/07-报告 |
| `原型/**` | PM 让你查原型时 | PM 明确要求新建/更新原型时;后续所有 HTML demo 统一放此目录,按模块分子目录 |

## 记录格式规范

**写入任何条目前,先读对应文件顶部的"规则"和"条目模板"**。每类记录有固定格式,必须按模板写,关键字段不能漏:

- **编号**:每条必须有唯一编号(进度-XXXX / 决策-XXXX / 沟通-XXXX / 知识-XXXX / 风险-XXX),编号连续递增
- **顺序**:新条目插在文件**最上方**(倒序),不要追加到底部
- **交叉引用**:涉及其他记录时必须标注编号(如"落实 决策-0012""由 沟通-0045 触发")
- **决策状态**:决策记录必须填"状态"字段(生效 / 已被 取代 / 已废弃);被推翻时更新旧条目状态,不删旧条目

## 真相源规则(避免信息打架)

| 信息类型 | 唯一真相源 | 其他文件只能做什么 |
|------|---------|---------|
| 里程碑当前状态 | `项目管理/路线图.md` | 进度日志只记"何时变更为何状态",不维护状态值 |
| 风险当前状态 | `项目管理/📊风险登记册（Base）` | 沟通记录只引用风险编号,不复制风险描述 |
| 干系人信息 | `项目管理/干系人矩阵.md` | 沟通记录只记参与方,不重复记录影响力/参与度 |
| 周报/月报 | `文档库/07-报告/` | 由记忆层生成,不要反过来把只在周报里的信息当唯一记录 |
| 决策内容 | `记忆/决策记录.md` | 其他文件只引用决策编号 |

**核心原则**:同一信息只在一个地方维护,其他地方只引用编号。

## 记忆维护规则

- **只追加不回改**:`进度日志`、`决策记录`、`沟通记录`、`知识库` 只追加新条目,不删改旧条目;修正用新条目并引用旧编号
- **决策状态可更新**:决策被推翻时,更新旧决策的"状态"字段指向新决策(这是唯一允许的旧条目修改)
- **归档由人决定**:按月/按阶段收口时,**提示** PM 是否归档,不自动剪切
- **时间戳**:每条记录写明日期(YYYY-MM-DD),沟通记录加具体时间,重要节点加时间戳

## 现状.md 特殊规则

- 这是**入口页**,不是新的信息源,是从其他文件提炼的"现在时快照"
- **可覆盖写**:不 append,只反映当下
- 更新时从 `路线图.md` 取里程碑、`风险登记册.md` 取最高风险、`进度日志.md` 取进行中事项
- AI 不主动更新现状,只在 PM 要求时更新

## 禁止事项

- 不主动创建本结构之外的文件
- 不编造决策、沟通内容、进度、决策状态
- 不在没有 PM 指示时修改 `项目管理/` 和 `文档库/` 下的文档
- 不引入流程引擎、状态机、质量门等自动化机制
- 不替 PM 做方向/优先级/方案决策,只整理选项供 PM 判断
- 不把同一信息写到两个真相源文件里(只引用编号)

## 协作建议

当 PM 描述一件事时,先判断属于哪类记录:
- 推进了什么工作 → 进度日志
- 做了一个选择 → 决策记录
- 开了会/聊了天/收了邮件 → 沟通记录
- 学到一个经验/踩了坑 → 知识库
- 要刷新当下焦点 → 现状(覆盖写)

判断不准时,问 PM:"这件事要记到哪个文件?" 不要默认猜。

写入前确认:"这个条目按模板填,编号是 XXXX,要引用哪些已有编号?" 避免 AI 自创编号或漏字段。
